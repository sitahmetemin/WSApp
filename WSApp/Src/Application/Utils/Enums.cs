namespace WSApp.Src.Application.Utils
{
    public class Enums
    {
        public enum StorageType
        {
            HardDisk,
            SSD
        }

        public enum OSName
        {
            FreeDos,
            Windows,
            Linux,
            MacOS
        }
    }
}
